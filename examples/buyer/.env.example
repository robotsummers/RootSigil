# Example buyer settings (testnet recommended)

# RootSigil base URL
ROOTSIGIL_URL=http://localhost:8080

# EVM private key for paying wallet (testnet only)
# Must be 0x-prefixed or hex without 0x (the script will normalize).
BUYER_PRIVATE_KEY=REPLACE_ME

# Optional: network for x402 scheme registration (default Base Sepolia)
NETWORK=eip155:84532

# Optional: override fetch timeout (ms)
TIMEOUT_MS=60000
